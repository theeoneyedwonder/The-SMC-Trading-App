# For local Linux/Mac dev only, run against the mock MT5 backend (SMC_MOCK=1).
# Mirrors requirements.txt minus MetaTrader5, which only ships Windows wheels.
# The real requirements.txt (used for the Windows PyInstaller build) is untouched.
fastapi==0.136.3
uvicorn==0.49.0
websockets==16.0
pandas==3.0.3
sqlalchemy
pydantic==2.13.4
plyer==2.1.0
python-dotenv==1.2.2
httpx>=0.27,<1
